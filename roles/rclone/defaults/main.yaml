---

rclone__configs:    "{{rclone.configs    | default([])}}"
rclone__config_dir: "{{rclone.config_dir | default(ansible_env.XDG_CONFIG_HOME ~ '/rclone')}}"
rclone__filters:    "{{rclone.filters    | default([])}}"

